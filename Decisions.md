# Decisions Log

A running log of decisions made during the Triple J Website project.

| Date | Decision | Reason |
|------|----------|--------|
| 2026-04-13 | Set up NotebookLM as document memory | Source-grounded answers, reduce hallucination |
| 2026-04-13 | Set up Obsidian as working memory | Persistent context across sessions, token efficiency |
| 2026-04-13 | Created Triple J Project folder on Desktop | Central file drop for project assets |

_Maintained by Claude Code_

| 2026-04-13 | Use Next.js + Supabase + Resend as backend stack | Free tier covers MVP; Postgres+RLS for security; no separate backend server |
| 2026-04-13 | 3-phase backend roadmap: MVP (lead capture) → Phase 2 (quotes+jobs) → Phase 3 (payments) | Ship fast, validate before building complex features |
| 2026-04-13 | Owner dashboard at /dashboard, Supabase Auth protected | Single owner account, row-level security on all tables |
| 2026-04-13 | Tokenized quote acceptance (no customer login) | Reduces friction — customer clicks Accept in email, no account needed |
| 2026-04-13 | SEO strategy: target hyper-local TX keywords competitors don't own | carports waco tx, welded carports central texas — zero competition on these |
| 2026-04-14 | Build public frontend in 3 phases (design system → homepage sections → form/gallery) | Ship incrementally, review after each phase before building next |
| 2026-04-14 | Use (marketing) route group to isolate public chrome from dashboard/login | Clean separation — Header/Footer only wrap public pages |
| 2026-04-14 | Design direction: WolfSteel-inspired, steel-blue #1e6bd6, no dark mode | Premium local contractor feel; dark mode doubles CSS work for zero audience benefit |
| 2026-04-14 | src/lib/site.ts as single source of truth for NAP, nav, services, cities | One edit point for all site-wide constants |
| 2026-04-14 | QuoteForm dimensions: W×L×H as 3 numeric fields, concat into message field | Captures structured size data without requiring a DB migration |
| 2026-04-14 | ClickUp as CRM: HOLD — revisit when live leads come in | Don't over-engineer before validating lead volume; Option 2 (ClickUp + custom quotes) preferred if/when needed |
| 2026-04-14 | Sandbox can't push to GitHub (proxy blocks outbound) | Julian must run `git push origin main` from his Mac Terminal |
| 2026-04-15 | Font system: Geist → Barlow Condensed (headlines) + Inter (body) | Industrial/confident feel; Barlow Condensed is tight and bold without needing custom weights |
| 2026-04-15 | Tagline locked: "Built right, built fast, built by Triple J." | Julian approved; replaces generic placeholder |
| 2026-04-15 | TrustBar stats: Zero Subcontractors / Welded or Bolted / Same-Week / Temple TX | More differentiating than generic "150+ Projects" stats |
| 2026-04-15 | "48-hour build" messaging removed site-wide | 48 hrs = materials arrival time, NOT build time. Misleading. Replaced with "same-week" language. |
| 2026-04-15 | "Custom welded" → "welded or bolted" everywhere | Triple J offers BOTH options. Saying only welded was losing bolted-interested customers. |
| 2026-04-15 | Added Lean-to Patios + House Additions as services | Real services Triple J performs; were absent from all service lists and nav |
| 2026-04-15 | Testimonials: auto-scroll marquee instead of static grid | More engaging; hides the fact that placeholder copy is thin until real reviews arrive |
| 2026-04-15 | inspiration/screenshots/ folder created | Drop zone for visual reference screenshots before next design pass |
| 2026-04-15 | Dashboard deployment on hold | Auth flow not yet built; Julian still needs to set 7 Vercel env vars |
| 2026-04-20 | "Triple J" name meaning logged: Juan + Julian + Jose Alfredo ("Freddy") | Vault didn't have it; Freddy is foreman with all jobsite skills, key person for SEO/marketing realism |
| 2026-04-20 | Lead Engine V1 picked as next build | Permit-scrape → Claude parse → Supabase pipeline → dashboard call list. Skip Apollo enrichment, skip auto-outreach, skip SEO + barndo engines for now. Validate lead quality before adding cost/automation. |
| 2026-04-20 | Lead Engine filter strategy = **B (stay close to wheelhouse)** | <$500K valuation, pole barns / ag / small commercial accessory / auto-storage. Skip large industrial PEMB / warehouse subcontractor pivot. Triple J needs realistic close-rate leads NOW, not commercial-pivot leads later. |
| 2026-04-20 | Renamed `/dashboard` → `/hq` and removed Owner Login from public Footer | Reduce attack surface and obscurity-by-naming. URL is no longer guessable; no public link points at the auth surface. Middleware still enforces Supabase auth at `/hq/:path*`. Julian to enable 2FA in Supabase Auth himself. |
| 2026-04-20 | Domain = **triplejmetaltx.com** (Squarespace registrar, ~$3 first-year promo) | Texas-local SEO wedge (`tx` signals state). Short, .com, matches brand. Migrating off Wix `TripleJJMetal.com` (two-J name didn't match brand identity). |
| 2026-04-20 | Email = **julianleon@triplejmetaltx.com** via Google Workspace Business Starter | Real Gmail inbox on custom domain. Industry standard, unlocks Gmail/Calendar/Drive/Meet on the brand. ~$7/user/mo. |
| 2026-04-21 | Lead Engine V1 MVP scope = **3 Revize jurisdictions** (Temple, Bell County, Harker Heights) | Static HTML + PDF, no headless browser, fits Opus 5-hour budget. CivicPlus jurisdictions (Killeen, Copperas Cove, Waco, McLennan Co., Belton P&Z) deferred to follow-up session — need Firecrawl + headless rendering. |
| 2026-04-21 | Capital Metal Buildings / Irvin Jimenez strategy = **Both: supply secondary + installer-partnership pitch** | MetalMax stays primary supplier (do NOT replace). Cap Capital at ≤30% of material spend for specialty/overflow. Separately pitch Irvin on installer-partnership angle — Capital's overflow jobs → Triple J crew. Two tracks, run in parallel. |
| 2026-04-21 | Financing provider = **Hearth primary, Enhancify subprime fallback** | Hearth = free to contractor, prime-biased lender pool (LightStream/SoFi), best partner portal. Enhancify activated only if Hearth decline rate >30% after 90 days. HFS / GreenSky / Service Finance Co. evaluated and deferred — merchant-loan fees (3–9%) don't justify the lift at current ticket sizes. |
| 2026-04-21 | 3D Builder = **deferred indefinitely** | Vendor iframe maybe someday once lead volume justifies the paid integration. No custom build — too much scope for a solo tech lead. Revisit post-Lead-Engine live. |
| 2026-04-21 | Facebook Marketplace = **active channel, reconnaissance pending Julian** | Irvin's highest-ROI channel (confirmed verbally). Julian to recon Capital's listings + category/pricing/photo formula between sessions. Next-session build: FB-to-`/quote?src=fb` funnel with UTM tracking + listing-template generator from `/hq/gallery` photos. Differentiation angle: welded-only, same-week, real crew photos — not stock renders. |
| 2026-04-21 | Financing integration moved from **Phase 2 → Phase 3** | Financing lifts conversion ~15-25%. Speed-to-response + review velocity lift it 100-300%. Julian also correctly flagged he's not ready to apply (Triple J ~1yr, likely lower caps + fees at Hearth). Revisit when lead flow is 20+/mo AND Triple J has 10+ Google reviews AND GL/WC insurance COIs are current. |
| 2026-04-21 | Phase 2 = **conversion + trust stack**, NOT financing | Between-session (Julian): claim GBP, manually collect 10 Google reviews from past customers, FB Marketplace recon. Next-session (Claude): Twilio SMS speed-to-response automation + post-job review-velocity automation. Pre-financing because #1 gap is review count (~0 vs. competitors' 20-100+). |
| 2026-04-21 | Permit data publishing strategy = **public/private split** | Public `/market-report/[year-month]` auto-generates aggregate stats (permits by city, valuation histograms, type breakdown, contractor names — all public record already). Private `/hq/permit-leads` keeps individual addresses, wheelhouse scores, reasoning, outreach status, notes. Publish the narrative, hoard the leads. |
| 2026-04-21 | Tech advantage framing = **18-36 month window, NOT permanent moat** | Permit-scraper + automation decays as Claude Code commoditizes. Real durable moats: brand, Google reviews, crew quality (Freddy), supplier relationships, bonding capacity, local trust. Strategy: use tech head start to aggressively convert to durable moats (reviews, crew, supplier leverage, capital) before the window closes ~2028. The scraper gets you in the door; everything after is the business. |
| 2026-04-21 | Outside capital stance = **debt-only, no equity dilution for foreseeable future** | Contractor grand slam = cash-flow the business, debt only for assets that earn (truck, welder, yard). SBA 7(a)/504 at Year 3+. Bonding capacity build from Year 2. NEVER merchant cash advances. NEVER give up equity pre-exit. If PE bolt-on offer comes at $5M+ revenue, evaluate — but Juan stays 100% owner until a real exit. |
| 2026-04-21 | Language division of labor = **Julian handles English, Juan/Freddy handle Spanish** | Julian is the only English speaker in the company (hence the sales role). Julian also holds 20% equity — NOT a commission arrangement as earlier noted. This is an unfair advantage, not a gap: Spanish-language landing page + Google Voice Spanish line is on the roadmap (deferred to post-Phase-2). Hispanic rural/ag market in Central TX is underserved — competitors aren't indexed for Spanish metal-buildings keywords. |
| 2026-04-21 (evening) | Ownership confirmed: **Juan 40% / Freddy 40% / Julian 20% (equity, not commission)** | LegalZoom formation (paperwork verify pending). Freddy is a co-owner equity partner, not just a foreman — explains crew stability + Spanish-customer trust + succession cleanliness. 20% equity > 20% commission because it's a cut of ALL net profit + any sale proceeds, forever. |
| 2026-04-21 (evening) | Real net margins are **2x textbook contractor assumption** | Small jobs 50-60% net / medium 35-45% / large 25-35%. Live data: Copperas Cove $8,600 job netted $5,464 (63%). Owner-operated welders + paid-off equipment + direct MetalMax + zero subs = lean cost structure. Wealth projections updated accordingly (10-yr cumulative to Julian: ~$7M distributions + potential $5-7M exit). |
| 2026-04-21 (evening) | Julian is a welder (not just sales/tech) | Team has 2 in-house welders (Julian + Freddy). Can scale to $3-5M revenue before hiring a 3rd welder. Juan = investor/relationships only, no jobsite labor. |
| 2026-04-21 (evening) | Phase 2 = **full execution, no cash-crunch delays** | Julian has ~$16K incoming from recent contracts. F350 at $900/mo, everything else paid off. Twilio, Anthropic, Google Voice all affordable. Previous cash-crunch constraint plan is obsolete. Deploy + GBP + reviews this week; Twilio automation next session; Spanish + pricing page session after. |
| 2026-04-21 (evening) | Restaurant reopen window = **mid-July 2026** | Mexicano Grill (812 E Central Ave, Belton TX) rebuild consumes Juan's cash + attention until mid-July. Triple J tech work in the ~10-week window; full Triple J refocus + cash reinvestment when restaurant opens. Window strategy: come out of stall with permit engine producing 30+ scored leads + 10+ Google reviews + 5+ market-report articles live. |
| 2026-04-21 (evening) | Domain migration Wix → triplejmetaltx.com = **COMPLETE** | Previously listed as pending. Site live on new domain + professional. Remove from all "future work" lists. |
| 2026-04-22 | SEO hygiene: `robots.ts` + `sitemap.ts`, `/privacy` + `/terms`, footer service URLs fixed (`metal-garages`), redirects for old `/services/garages` and unbuilt lean-to / house-addition slugs → `/contact` | Code-side audit fixes; lean-to + house-addition still need real `/services/[slug]` pages when copy is ready. Julian still must verify **Vercel production domain** points at this project (www was serving 404+noindex at audit time). |
| 2026-04-22 | Email sender unified to `@triplejmetaltx.com` | Previously quote-send route used `@triplejmetalllc.com` (3 L's, no `tx`) which didn't match the verified Resend domain. Fixed to `quotes@triplejmetaltx.com` + `reply_to: julianleon@triplejmetaltx.com`. Google Workspace aliases (`sales@`, `support@`, `info@`, `julianleon@`) stay as inbound aliases — unrelated to Resend outbound sending. Outbound uses `leads@`, `quotes@`, `no-reply@` on the same domain. |
| 2026-04-22 | Manual customer creation enabled | `+ New Customer` button on `/hq/customers`. Previously customers could only be created via lead conversion, which blocked walk-ins, phone calls, and referrals. POST `/api/customers` already existed from earlier in session; added `NewCustomerForm.tsx` + table toggle. |
| 2026-04-22 | Manual `Run Scrape Now` button added to `/hq/permit-leads` | `/api/cron/scrape-permits` now supports dual auth: `Bearer CRON_SECRET` for Vercel Cron + Supabase cookie for `/hq` UI. Daily 14:00 UTC cron unchanged. |
| 2026-04-22 | `/hq` home redesigned as 9-card KPI grid | Replaced 4-card lead-status row with grouped sections (Pipeline · Conversion · Revenue · Operations) drawing from `leads`, `customers`, `quotes`, `jobs`, `permit_leads` in parallel. Lead-pipeline pill row kept as a second tier. Numerical only — graphs/charts deferred to Phase B. |
| 2026-04-22 | QuoteForm `<form>` element removed entirely | Converted to `<div>` with `onClick` handler. Eliminates browser implicit-submit from Enter key, autofill chains, iOS "Go" button. Fixed earlier bug where the form auto-submitted after Step 2 skipping concrete/timeline/military. |
| 2026-04-22 | Lead → Customer conversion + delete-with-confirm on `/hq` | `→ Customer` button creates a customer from a lead and flips lead status to `quoted`. Delete uses two-click confirm pattern. Unblocks quote builder (customer dropdown was empty). |
| 2026-04-22 | Phase B = dedicated design session | Separate from today's functional pass. Scope: HQ nav redesign to steel-blue, Recharts/Tremor graphs + sparklines, funnel chart, public-site hero + Spanish `/es/` landing, pricing page, quote templates UI. Component-library options: Tremor (base), 21st.dev (accent patterns), shadcn/ui charts (wrapper). |
| 2026-04-23 | **Reversed:** "no dark mode" → dark mode follows iOS `prefers-color-scheme` | Was decided 2026-04-14 to skip dark mode. Reversing because HQ is now mobile-first (iPhone 16 Pro + crew job-site phones) being built into a real installable PWA. Dark-by-system-preference is the iOS-native default. No user toggle. Steel-blue brand stays; refined for dark contrast (`--brand-fg: #4d8dff` in dark, `#1e6bd6` in light). Implementation via semantic CSS tokens (`--surface-1`, `--text-primary`, etc.) that remap inside `@media (prefers-color-scheme: dark)`. |
| 2026-04-23 | **"Welded" at Triple J ALWAYS means welded + bolted** (engineering reality, not marketing framing) | Per Julian: to weld red-iron framing on-site, the crew first bolts everything together so it's sturdy and held in correct position — then welds. The bolts STAY in (don't get removed) because each bolt has a rubber gasket; pulling them would leak. So every "welded" Triple J build is structurally welded + bolted. "Bolted" jobs are just bolts (no welding). Implication for copy: never claim welds replace bolts; the engineering message is "welded structures are reinforced with permanent bolts" — a credibility upgrade, not a contradiction of the "welded or bolted" tagline. Worth touching on the new Hybrid Projects page and reviewing the existing welded-vs-bolted blog post. |
| 2026-04-23 | New page: `/services/hybrid-projects` — catch-all showcase for non-standard builds | Triple J does projects that don't fit cleanly under carports/garages/barns: horse stalls, all-black warehouses, decks, custom commercial. Page is a marketing-narrative wrapper that auto-pulls photos from `gallery_items` where `type = 'Hybrid'`. Slug `/services/hybrid-projects` (not `/hybrid-projects` top-level) for SEO inheritance + nav consistency. Rolling this out before the broader gallery SEO multipliers plan (`gallery-seo-multipliers.md`) so Julian has a sales-link URL for commercial leads immediately while the multiplier plan ages until photos are uploaded. |
| 2026-04-23 | New page: `/partners` (B2B install-partnership funnel) — main nav | Promoted from "footer-only" to main nav after testing because suppliers/manufacturers/GCs don't expect a contractor to have a real B2B page; tucked-under-footer wasn't going to land. Form posts to new `partner_inquiries` table (separate from retail leads — different fields, different ops cadence). Owner email + sender confirmation + push notification on submit. HQ inbox at `/hq/partners` with status pills (new/contacted/engaged/declined) and notes autosave. Footer link removed as duplicate. Migration `011_partner_inquiries.sql` applied to prod. |
| 2026-04-23 | Service Areas demoted from main nav → footer-only | Adding Partners pushed nav to 7 items. Service Areas was the right one to demote because the footer already has a dedicated Service Areas column listing individual cities — main-nav overview link was redundant. Footer column gains a "View all service areas →" overview link at the bottom (mirrors the Services column "View all" pattern). |
| 2026-04-23 | **Public positioning shift: supplier-agnostic** | Removed all customer-facing references to MetalMax (primary supplier), MetalMart, WeatherXL™, Turnium / Sheffield (panel-line brands), and MaxLoc / MaxSeam / MaxSnap (concealed-fastener product names) across site copy, blog posts, location pages, service-page data, and vault docs. Reason: naming a single supplier publicly locks the company into one upstream relationship in the eyes of partners, customers, and Google. Suppliers churn, terms change, exclusivity gets weaponized. Triple J reads as "leading regional Texas suppliers — multi-source" so we can rotate, dual-source, or add suppliers without any web rewrite. **Operational note:** internal supplier choices stay flexible and live in operational tools (purchase orders, invoices) — NOT in the vault, public copy, or AI-facing files. The `gallery_items.panel_color_line` DB column still uses internal IDs `'turnium' \| 'sheffield'` for back-compat; user-facing labels are "Standard Line" / "Premium Line" via `LINE_LABELS` in `src/lib/colors.ts`. Swatches still load from `metalmax.com` URLs via `getSwatchUrl()` — known caveat; mid-term mirror to Supabase Storage. Multi-supplier color survey (MetalMart, others) is a deferred follow-up. |
| 2026-04-23 | **Lone-Star color names become canonical** (one name inside + outside) | Replaced the dual-name display (technical name + italic alias) on `/services/colors` with a single canonical name per swatch. All 39 colors now have a Texas-evocative name (Storm Cloud, Wine Country, Pinto Green, Saddle Tan, Bell County Black, etc.) used EVERYWHERE — public site, HQ color picker, alt text, gallery cards. When a customer says "Storm Cloud", Julian/Freddy see "Storm Cloud" in HQ with no translation. `PanelColor.legacyName` field retained in source only (never rendered) so `resolve()` in `gallery-colors.ts` still matches any existing `gallery_items` rows that stored the previous names. DB slugs unchanged. |
| 2026-04-23 | **Galvalume + Acrylic-Coated Galvalume kept as functional names** (exception to the Lone-Star rename) | These two are Triple J's cheapest panel option — ~$0.50–$1 less per sheet. Customers who specifically ask for "Galvalume" are signaling price-sensitivity. Renaming them to "Bare Iron" / "Bare Iron — Sealed" hid the pricing signal customers actually shop on. The other 37 colors keep their Lone-Star names (those are decorative choices where evocative naming adds value); these two stay technical. **Do NOT re-rename in future Lone-Star naming passes.** |
| 2026-04-23 | **Pre-launch security pass: hCaptcha + per-IP rate limiting + Harker Heights "48-hour" copy fix** | Layer 2 of the approved security plan shipped. **hCaptcha** chosen over Cloudflare Turnstile (Julian's call — same purpose, free, identical UX). Site key + secret in `NEXT_PUBLIC_HCAPTCHA_SITE_KEY` + `HCAPTCHA_SECRET_KEY` (Sensitive). Server-side verification via `src/lib/captcha.ts`; client widget via `@hcaptcha/react-hcaptcha`. Skip-if-unset pattern for dev. **Per-IP rate limiting** via `src/lib/rate-limit.ts` (in-memory LRU, single warm Vercel instance scope — spam deterrent only, not a DDoS shield): 5/hr on `/api/leads`, 3/hr on `/api/partner-inquiries`. **Harker Heights** `metaTitle` + `heroHeadline` `48-hour` → `same-week` (was contradicting Decisions.md 2026-04-15 lock). **CSP flip from Report-Only → enforced** intentionally deferred to a separate commit after 24h of report monitoring. **Secret hygiene:** the hCaptcha secret was sent through chat — must rotate in hCaptcha dashboard + re-add via `vercel env add HCAPTCHA_SECRET_KEY production --sensitive` once integration is verified end-to-end. |
| 2026-04-23 | **Belton personalized — county-seat authority + permit moat + ranch reach + 2 callouts (template field migrated `premiumCallout` → `callouts[]`)** | Twenty user design decisions for Belton. Coherent positioning: county seat = where Bell County's permits get pulled. **Picks:** hero photo = Bell County Courthouse (1885 limestone, most iconic Belton image); headline = "Built in Belton. / **Bell County's home crew.**"; distance stat = "📍 10 mi south · 15 min from HQ" (closest city to HQ is a real selling point); `habla` = true (Spanish chip); positioning = county seat authority ("where Bell County's permits get pulled"); landmarks = Bell County Courthouse + Lake Belton/BLORA + Pendleton/Rural Ranch Country (3-card grid covering civic + lake + rural identity); neighborhoods = Lakeshore Drive + Heritage Place + North Belton + Pendleton + Sparta + 'all of Bell County'; intro tone = county-seat authority opener with railroad-style heritage line ("The 1885 courthouse still stands. We build with that kind of intention."); top services = Range trio (Carports + Barns + RV Covers — covers residential + ranch + lakeside); why-local bullets = county-seat authority heavy (permit office knowledge + 15-min proximity + Bell County clay + welded-or-bolted choice); UMHB = skipped per Q11 (didn't make landmark cut); permit authority featured in intro AND a why-local bullet; lake angle = light (landmark only, doesn't dilute county-seat focus); proximity = dedicated bullet ("15 min from our shop — fastest install in Bell County"). **2 CALLOUTS STACKED:** (1) Permit Advisory FIRST — concise + practical tone ("Bell County permits handled. Included in your contract — no 4-week back-and-forth.") → /contact. (2) Ranch & Agricultural SECOND — "Built for Bell County's ranch country." → /services/barns. **TYPE MIGRATION:** `LocationData.premiumCallout?: CityCallout` (singular) → `LocationData.callouts?: CityCallout[]` (array). Multiple callouts stack vertically in array order with per-callout stagger reveal. Temple's single callout migrated to wrapped array form (zero behavior change). Template iterates over `loc.callouts`. **PHOTO TODO** for Belton: real Bell County Courthouse + Lake Belton + Pendleton ranch + downtown Belton photos still need sourcing into `/public/images/locations/belton/`. Hero placeholder = `/images/double-carport-install.jpg`; landmarks render as typography-only cards until photos arrive. |
| 2026-04-23 | **Temple personalized — HQ-pride + lakeside lifestyle + new `premiumCallout` section** | Twenty user design decisions captured via AskUserQuestion (8 personalization + 12 page-shape, since template was already locked from Killeen). Combined positioning: lakeside-lifestyle hero photo + HQ-pride intro + residential bread-and-butter services + HQ-heavy why-local bullets. **Picks:** hero photo = Lake Belton (lifestyle); headline = "Built in Temple. / **Built where we live.**"; distance stat = "0 mi · Where we live" (HQ city); `habla` = true (Spanish chip); positioning = lakeside lifestyle (matches photo); landmarks = Lake Belton + Santa Fe Depot + Scott & White (3-card grid, full Temple identity); neighborhoods = Western Hills + Lake Belton/Lakeside + Sammons Trail + Stagecoach Trail + 'all of Temple'; intro tone = HQ-pride opener that mentions shop address + railroad-town tip ("Temple's a railroad town. We weld like one.") + lake mention + Spanish line; top services = Carports + Metal Garages + Turnkey + Concrete (residential-led); why-local bullets = HQ-pride heavy (shop on Tem-Bel Lane, lake gusts, Bell County clay, same-week); healthcare = light landmark only; lake angle = strong (intro + bullet); skip "Drop by shop" CTA (consistent with other cities); HQ address featured in hero stat + body copy; railroad heritage = light single sentence; affluent angle = direct via new "Premium Residential" callout; military commuters = skip. **NEW DATA TYPE: `CityCallout`** + new optional `premiumCallout?` field on `LocationData` — eyebrow + headline + blurb + ctaLabel + ctaHref. Renders as a brand-blue tinted standalone section between Services and Why-local. Temple's callout: "Premium Residential" / "Built for Temple's HOA-grade neighborhoods." / Western Hills + Heritage Acres + Lake Belton subdivision blurb / "See HOA-compliant builds →" → `/services/hoa-compliant-structures`. Reusable for other cities with secondary-market angles (e.g. Lampasas ranch/ag, future). **PHOTO TODO** for Temple: real Lake Belton hero + Santa Fe Depot + Scott & White + downtown Temple landmark photos still need sourcing from Unsplash/Pexels into `/public/images/locations/temple/`. Hero placeholder = `/images/carport-gable-residential.jpg`; landmarks render as typography-only cards (intentional magazine treatment) until photos arrive. Swap is a 2-line edit per landmark. |
| 2026-04-23 | **`/locations/[slug]` template redesigned + Killeen personalized as the proof-of-concept** | Twenty user design decisions captured via AskUserQuestion locked the direction (12 template-level + 8 Killeen-specific). **TEMPLATE (applies to all 14 cities):** (1) Hero = full-bleed city-landmark photo + diagonal dark gradient (matches PreFooterCta/QuoteForm language). (2) Headline = per-city custom two-line (line 2 in brand-blue) — `customHeadline` field overrides legacy `heroHeadline`. (3) Drive time from Temple HQ featured prominently as a hero stat with pin icon (e.g. "25 mi southwest · 30 min from HQ"). (4) County identity = red eyebrow pill in hero, optional "Hablamos Español" brand-blue chip alongside it (gated by `habla` flag). (5) Section order: Hero → Local intro + Landmarks → Where We Build (neighborhoods) → Services → Why local (dark) → Gallery → Conditional military section → PreFooterCta+Footer. (6) Landmarks = dedicated 3-card grid with photos (or 2 when only 2 are picked); typography-only fallback card with brand-blue numbered accent + dot-grid texture when no photo sourced. (7) Neighborhoods = visual chip grid in dedicated "Where We Build" section. (8) Competitor comparison table DROPPED — redundant with the homepage WhyTripleJ. (9) Services = 3-up photo card mini-grid (top 3 per city via new `topServices` field) + "See all services →" link, reusing the homepage Services photo treatment. (10) Why local = shortened to 4 punchy bullets via new `whyLocalBullets` field, rendered on a dark editorial spread + brand-blue glow (matches WhyTripleJ language). (11) Soil/climate authority woven into Why local bullets (one bullet specifically calls out "4,000 PSI concrete for {county}'s expansive clay soils"). (12) Gallery = generic recent-builds strip (4-6 photos, NOT city-filtered) pulled from `gallery_items`. **Trust strip + competitor table + bottom QuoteForm DROPPED** (redundant with TrustBar / WhyTripleJ / PreFooterCta). **DATA MODEL:** `LocationData` extended with new optional fields — `heroImage`, `heroImageAlt`, `customHeadline`, `heroSubhead`, `distanceFromTemple`, `habla`, `localIntro`, `landmarks[]` (new `Landmark` type with optional `imageSrc`), `neighborhoods[]`, `topServices[]`, `whyLocalBullets[]`. All optional with legacy-field fallbacks so the other 13 cities still render with the new template even before they're personalized. **KILLEEN SPECIFIC:** military-first positioning (per Q14), hero photo placeholder for Fort Cavazos main gate (TODO: source from Unsplash), `customHeadline` = "Built in Killeen. / Built for Fort Cavazos." (brand-blue accent on line 2), `distanceFromTemple` = "25 mi southwest · 30 min from HQ", `habla` = true (Spanish chip in hero), `localIntro` opens with "Killeen runs on Fort Cavazos. PCS season hits, hail season hits...", landmarks = Fort Cavazos + Stillhouse Hollow Lake (typography-only cards until photos sourced), neighborhoods = Cedar Ridge / Heritage Park / Westcliff / Rosewood Heights / All of Killeen, topServices = carports + rv-covers + metal-garages, whyLocalBullets = 4 lines woven with the soil callout, military section = dark editorial spread + brand-blue glow with refined "Fort Cavazos Timelines — Built In" headline. **PHOTO TODO:** real Killeen hero (Fort Cavazos main gate) + landmark photos (Fort Cavazos + Stillhouse Hollow) need to be sourced from Unsplash/Pexels and dropped into `/public/images/locations/killeen/` — the data structure supports any URL, swap is a 2-line edit. Until then, hero uses `red-iron-frame-hero.jpg` + landmarks render as typography-only cards (intentional magazine treatment, not broken). |
| 2026-04-23 | **`/services` list page redesigned — magazine hero + featured flagship + 3-up grid + Resources card** | First page in the multi-page redesign queue. Four user design decisions: (1) Hero = full-bleed photo backdrop + dark gradient (matches homepage hero language) — `red-iron-frame-hero.jpg` + diagonal `from-black/95 via-black/80 to-brand-700/40` overlay + red eyebrow pill ("WHAT WE BUILD") + Barlow huge "Six things we build. / **Built whole, by us.**" + subhead, **no CTAs in hero** (the cards below are the CTAs). (2) Cards = featured flagship layout — Carports gets a full-width split card (60/40 photo-left / content-right on lg) with red "FLAGSHIP" eyebrow + Barlow `text-6xl` title + first 3 `features[]` bullets + brand-blue "See Carports details →"; the other 5 services in a 3-up grid below with photo-dominant treatment matching the homepage Services section. (3) "Gap 1/2/3/4" keyword-gap chips dropped — they were internal SEO research surfaced to customers. (4) Full `<QuoteForm />` at bottom dropped — `<PreFooterCta />` in the marketing layout already closes every page. **Also dropped:** the 4-up dark trust strip (redundant with homepage TrustBar + magazine footer) and the SEO helper-links strip (absorbed into a new dark "Resources" card that sits as the 6th cell in the grid, linking to `/services/colors`, `/services/pbr-vs-pbu-panels`, `/locations`). **Photos:** carports → carport-gable-residential, turnkey → carport-truck-concrete-hero, garages → metal-garage-green; barns / rv-covers / hoa-compliant use closest-fit placeholders with heavier gradient (flag for swap when better photos land in `/public/images/services/`). **Per-card stagger reveal** via `<Reveal delay={i * 80}>`. `SERVICES` data shape preserved — no changes to `src/lib/services.ts`. Page is ~70% leaner than before; PreFooterCta + Footer (always-on in marketing layout) handle the closer. |
| 2026-04-23 | **QuoteForm redesigned — cinematic glass card, 2-step (revising 3-step lock), magazine editorial labels, service-chip-first, redirect to /thank-you** | Fifteen user design decisions captured via AskUserQuestion locked the direction. **REVISIONS to prior locks:** (a) The 2026-04-15 "multi-step lead form: ZIP → service+type+dimensions → concrete+timeline+military" lock is **revised from 3 steps to 2 steps**. New split: Step 1 = service-chip + structure + dimensions + ZIP; Step 2 = name + phone + email + concrete + surface + timeline + message + captcha + submit. Reduces friction; opens with a visual chip selector instead of cold text fields. (b) The "QuoteForm not animated" carve-out from the 2026-04-23 motion language lock is **revised** — step transitions now slide-in from the right (300ms, magazine ease-out, `.step-slide-in` keyframe added to globals.css). The reveal-on-scroll exclusion still holds; only the step-change animation is added. **NEW DESIGN:** (1) Background = full-bleed `red-iron-frame-hero.jpg` photo + diagonal dark gradient (`from-black/95 via-black/80 to-brand-700/40`), bookending the hero atmospheric language. (2) Layout = centered narrow single column (`max-w-xl`). (3) Form container = translucent glass card (`bg-white/[0.04] + backdrop-blur-md + border-white/10 + shadow-2xl`). (4) Header = "quieter" magazine treatment (red eyebrow pill "Get a Quote" + Barlow `text-3xl/4xl` + brand-blue accent on second line — smaller than hero/Services). (5) Step indicator = slim 1px brand-blue progress bar (50% / 100%) above the form, with eyebrow text "Step N of 2 · {label}". (6) Inputs = outlined-dark (`border-white/15 bg-white/5 text-white`, brand-blue ring on focus). (7) Service chips = 2-up grid with photo thumbnails + dark gradient overlay + checkmark badge when selected — first thing visible. (8) Field labels = magazine editorial (`font-display extrabold uppercase tracking-[0.18em] text-[11px] text-white/60`) with bullets bracketing the text. (9) Trust signals = BOTH eyebrow trio above form ("Free Quote · Reply within 24h · 150+ Central Texas Builds") AND inline reassurance below submit ("Free quote — no spam, no obligation. Most replies within 24 hours."). (10) Phone hidden in form (already in header/footer/mobile call bar). (11) Submit label = "Send to Triple J" (was "Request My Free Quote"). (12) Success = redirect to new `/thank-you` page (was inline success card). **NEW PAGE: `/thank-you`** with `robots: { index: false }`, the homepage hero language reused (red-iron photo + ken-burns-less hero gradient), brand-blue check badge instead of red pill, "Got it. / We'll be in touch." headline, then a 3-step "What happens next" section. **Discount visibility** = single eyebrow line above form ("Military, first-responder & trade discounts honored") — discount applied during the call, no checkbox. `is_military` field still in payload (defaults to `false`) so backend stays compatible. **Anti-implicit-submit lock preserved**: still `<div>` not `<form>`, `onKeyDown` handler advances step on Enter (not in textarea). hCaptcha integration preserved with `theme="dark"` for the glass card. |
| 2026-04-23 | **Site chrome redesigned: Header + Footer + new pre-footer CTA band** | Five user design decisions captured via AskUserQuestion locked the direction. **(1) Header is now route-aware:** on `/` (homepage) it's transparent over the dark hero at scrollY≈0, then transitions to dark `ink-900/85 + backdrop-blur-md + shadow` after 80px scroll. On every other route it stays dark+blur from load (ink-900) so it remains visible over light content (text pages, list pages). Implementation reads `usePathname()` and conditions both states. **(2) Top thin bar simplified:** dropped phone+hours+address; now shows phone (left) + brand-blue badge "⚡ Same-Week Installs · Central Texas" (right). Hides on scroll OR in transparent mode. **(3) Wordmark upgraded to magazine lockup:** Barlow Condensed extrabold uppercase "Triple J" with brand-blue " / Metal" descender, plus uppercase tracking-[0.2em] sub-line "Temple · Central Texas." Lion bumped to 48px. **(4) Active nav link** gets a brand-blue underline that scales from 0 → 1 on hover (animated), or sits at scale-1 when `aria-current="page"`. **(5) New `<PreFooterCta />` band** sits between `<main>` and `<Footer>` in the marketing layout — full-bleed photo backdrop (`carport-gable-residential.jpg`) + diagonal dark gradient (`bg-gradient-to-tr from-black/95 via-black/75 to-brand-700/40`) + red eyebrow pill ("Built for this") + Barlow huge "Ready to build? / **We're ready to start.**" (brand-blue accent on punch line) + primary button + ghost phone link. Bookends the hero on every marketing page. **(6) Footer brand column upgraded to magazine-bold lockup:** spans 2 cols on lg, 56-64px lion + Barlow `text-5xl` "Triple J" + brand-blue `text-4xl` "Metal" descender + locked tagline ("Built right, built fast, built by Triple J.") + family signature line ("Family-owned · Founded 2025 · 150+ jobs across Central Texas") + NAP block with brand-blue pin/phone icons. Other footer column headers flipped from white → brand-blue (`text-brand-400`) for accent rhythm. Bottom strip gains a Partners link alongside Privacy/Terms. **Mobile drawer typography upgraded** to Barlow extrabold uppercase `text-2xl` for nav items (was `text-lg` regular). |
| 2026-04-23 | **ServiceAreas section redesigned — detailed cards + dot-grid texture + HQ feature** | Previous: bare 5-up city cards with pin icon + name + generic blurb on a default-light section. New treatment keeps the white background but adds detail and structure: **(1) Subtle dot-grid texture** ornament (new `.bg-dot-grid` utility, 22px radial pattern, 6% opacity) — different from Services' line grid for visual variety. Plus a soft brand-blue radial wash in the upper-right for depth without changing the white read. **(2) Magazine header** (red eyebrow pill + Barlow Condensed huge with brand-blue accent on "Central Texas.") matching hero/Services/WhyTripleJ/Testimonials. **(3) Featured Temple HQ card** spans 2 cols × 2 rows on lg, gets a brand-blue tinted gradient bg, oversized Barlow city name (`text-7xl`), red HQ badge, the actual shop address, and an extra blurb. **(4) Detail per card**: county eyebrow on top, distance from Temple ("10 mi south", "25 mi southwest"), 2 service tag chips (Carports / Garages / Military / HOA / RV Covers / Barns), pin icon corner accent, "See {city} →" footer link with hover slide. **(5) Per-card stagger reveal** (~80ms apart) when the grid enters viewport. **Data decoupled from `src/lib/site.ts` `SERVICE_CITIES`** — area metadata (county/distance/tags) lives in the component since it's homepage-specific richer data; `SERVICE_CITIES` stays the lean cross-site constant. |
| 2026-04-23 | **Testimonials section redesigned + placeholder hygiene fixed** | Previous: light bg, white cards with thin gray borders, every card name said "Placeholder Customer," and a footer disclaimer line ("Real Google reviews will replace these placeholders") leaked unfinished work onto the live site. New treatment: **dark `ink-950` editorial spread** (breaks the run of light at page-bottom: Gallery → Testimonials → ServiceAreas → QuoteForm), oversized outlined `"` glyph as bg ornament at 3% opacity, magazine header (red eyebrow pill + Barlow Condensed huge with brand-blue accent on "by Central Texans."), translucent dark cards (`bg-white/[0.03] + border-white/10`) with brand-blue corner quote glyph + amber stars + slightly bigger quote text. **Placeholder hygiene:** dropped per-card "Placeholder Customer" string and dropped the on-page disclaimer. Replaced with `attribution` field — short project + city tag like "Welded carport · Temple, TX" under a brand-blue "Verified Project" eyebrow. Reads credible while we wait for real Google reviews; swap pattern stays simple (just edit the REVIEWS array). `testimonials.md` template updated to match the new `{ quote, attribution, rating }` shape. **Marquee mechanic + pause-on-hover preserved per 2026-04-15 lock.** |
| 2026-04-23 | **WhyTripleJ section redesigned — dark editorial spread + decorated 2-panel comparison** | Previous treatment was a paper-2 light section with a cramped 3-column comparison table next to a portrait photo. New treatment: **(1) Background flipped to dark `ink-900` with the same brand-blue radial glow used by `.quote-glow`** — sits between Services (light) and HowItWorks (dark) as part of the "deep dive" dark zone. **(2) Header upgraded to magazine treatment**: red eyebrow pill ("WHY TRIPLE J") + Barlow Condensed huge white headline ("Welded or bolted —" with brand-blue accent on "your call.") matching hero/Services. **(3) Photo callout** moved from cramped portrait to full-width 16/6 hero strip with caption block on the left half. **(4) "Decorated graph"**: replaced 3-col table with two side-by-side panels — Triple J panel (brand-blue tinted gradient + white border + circle ✓ icons + bold white values) vs Other Co. panel (translucent white/2.5 + circle × icons + muted white/55 values). Each panel header carries its own micro-tagline ("Built whole, by us." vs "Kit + handoff."). **(5) Readability**: comparison values bumped from `text-sm` → `text-base md:text-lg`; subhead from `text-lg → text-lg sm:text-xl`. Same 6 comparison data rows preserved verbatim. |
| 2026-04-23 | **Homepage motion language locked: restrained CSS-only animations** | Two layers shipped, ~zero JS bundle delta. **(1) Hero entrance choreography** fires on page load — red eyebrow pill at 100ms, four headline lines stagger 250→610ms (~120ms apart), subhead at 850ms, CTAs at 1000ms, trust line at 1200ms; total ~1.7s. CSS `@keyframes hero-rise` (fade + 12px slide-up), per-element timeline keyed off `data-hero-step="N"` so all timing lives in `globals.css`. Ken-burns bg keeps drifting under the choreography — two layers of motion, neither distracting. **(2) Scroll-triggered reveals** on TrustBar, Services (header + per-card 80ms stagger), WhyTripleJ (header + photo + comparison columns), HowItWorks (header + step cards 100ms stagger), Gallery header, ServiceAreas header. New client hook `src/lib/use-reveal.ts` (one-shot IntersectionObserver, disconnects after first hit) + wrapper `src/components/ui/Reveal.tsx` (server-component-safe to host as a child). **Single easing curve everywhere:** `cubic-bezier(0.22, 1, 0.36, 1)` (magazine ease-out, no overshoot). **All animations respect `prefers-reduced-motion`** via comprehensive guard in globals.css. **No framer-motion in public bundle** — stays HQ-only via lazy-loaded Sheet. **QuoteForm explicitly NOT animated** (form fields shouldn't wait on motion). **One-shot reveals** — once revealed, stay revealed. |
| 2026-04-23 | **Homepage hero v2 redesigned + "48-hour" lock from 2026-04-15 finally fully executed** | The 2026-04-15 lock said "48-hour messaging removed site-wide" but was never executed on the homepage hero (the centerpiece). This pass closes that gap completely. **New hero design language locked:** centered full-bleed dark background with photo + heavy multi-stop gradient (`linear-gradient 135deg, rgba(0,0,0,.92) → rgba(15,23,42,.85) → rgba(30,107,214,.25)`), Barlow Condensed huge headline (`text-9xl` at lg), red-pill eyebrow ("BUILT IN TEXAS"), single primary brand-blue CTA + ghost phone link. **Strict hero color palette: white text + brand-blue (`#1e6bd6`) + ONE red pill + dark gradient bg.** No additional accent colors permitted. **Headline locked: "BUILT RIGHT. BUILT FAST. BUILT ACROSS CENTRAL TEXAS."** — three-beat rhythm, echoes the 2026-04-15 tagline lock, uses "fast" as honest vibe, no specific time promise. **Photo strategy:** existing `/images/red-iron-frame-hero.jpg` as bg today; future hero shots drop into `public/images/hero/` for a one-line swap. Gradient treatment stays even when better photos arrive. **Subtle ken-burns motion** (60s alternate cycle, respects `prefers-reduced-motion`) added via `.bg-kenburns` utility in globals.css. **Inspiration approved (visual fidelity only, not copy):** YETI atmospheric eyebrow-pill pattern + Mortenson magazine-style spacing. **Owner names DO NOT appear in the hero** (rejected v1 — owner intro belongs on `/about`). **Tone-reset (same commit) — 7 downstream urgency-coded copy spots fixed:** WhyTripleJ "Days, not months" → "Same-week scheduling"; HowItWorks "Three steps. One weekend. Done." → "Three steps. One company. Done."; QuoteForm "ASAP / 48 hrs" enum → "ASAP — this week if possible"; QuoteForm step-3 explainer "Materials arrive within 48 hrs" → "Materials arrive fast"; `/api/leads` ASAP label string updated to match; Testimonials placeholder copy ("this fast" / "two days") rewritten to lean craft + reliability; blog post title "How Military Families Get a Metal Carport in 48 Hours" → "...on Military Timelines" (slug unchanged for SEO stability). **Grep audit verified: zero `48.hour\|48 hr\|under 48\|one weekend\|two days\|this fast\|48hrs` matches in src/.** |
| 2026-04-24 | **HQ Phase 3 design-language lock 1: Barlow Condensed scoped to marketing only** | Phase 1+2 HQ screens leaked the magazine display face into iPhone PWA headings because `globals.css` had `h1, h2, h3 { font-family: var(--font-display) }` at `:root` level. With `/hq/layout.tsx` wrapping in `font-(--font-ios)`, the global rule won by specificity and every `<h2>` on More / Stats / Quotes Wizard rendered in heavy condensed type. Fix: scoped the rule to `.marketing h1, .marketing h2, .marketing h3` and added `className="marketing"` to `src/app/(marketing)/layout.tsx`. HQ headings now inherit the iOS stack automatically. Marketing site unchanged. HqHeader's explicit `font-(--font-ios)` stays (belt-and-braces). |
| 2026-04-24 | **HQ Phase 3 design-language lock 2: two-rule tap feedback** | Phase 1-3 code was mixing `active:scale-95`, `active:scale-[0.99]`, `active:scale-[0.98]`, and `active:bg-(--surface-3)` interchangeably with assorted `transition-transform` / `transition-colors` pairings. New locked utilities in `globals.css`: **`.tap-solid`** (transform scale(0.95) on active, 150ms cubic-bezier(0.22,1,0.36,1), transitions transform + bg + border + opacity together so callers don't need to stack transition-colors manually) for SOLID buttons + CTA tiles, and **`.tap-list`** (background flashes to `--surface-3` on active, transitions bg + color) for LIST rows, grouped rows, message rows, cards. Destructive buttons may combine `.tap-solid` with red active:bg. Both respect `prefers-reduced-motion`. 39 call sites migrated in one sweep; no more ad-hoc micro-interactions. |
| 2026-04-24 | **HQ Phase 3 design-language lock 3: interactive brand token is `--brand-fg`** | Raw `bg-brand-600` (fixed #1e6bd6) vs theme-aware `bg-(--brand-fg)` (lifts to #4d8dff in dark for contrast) were being used interchangeably. In dark mode the raw scale reads as a dim steel blue on top of OLED black. Locked: all HQ interactive brand usage uses `bg-(--brand-fg)` / `text-(--brand-fg)` / `border-(--brand-fg)` / `hover:bg-(--brand-fg-hover)` / `focus:ring-(--brand-fg)`. Raw `brand-600` reserved for marketing + decorative accents. Sweep swept 25+ call sites across `src/app/hq/**` and `src/components/hq/**`. Tailwind v4 alpha modifier (`bg-(--brand-fg)/15`) works for tinted fills. |
| 2026-04-24 | **HQ Phase 3 design-language lock 4: Lucide strokeWidth=2 default in HQ** | Phase 1-3 code had icons at 2 / 2.2 / 2.3 / 2.4 without a rule; visual weight drifted screen-to-screen. Locked: HQ default is `strokeWidth={2}`. Two intentional exceptions preserved: BottomTabBar active-state tab icon (`active ? 2.3 : 2`, pressed-state heavier weight) and HqHeader Plus CTA (`2.3`, CTA affordance). Everything else got swept to `{2}`. No visual regression — 2.2/2.3/2.4 were imperceptible drift, not intentional emphasis. |
| 2026-04-24 | **HQ Phase 3 Gallery restyle — semantic-token sweep + Lightbox locked easing + dark-mode parity** | The uncommitted Phase 3 Gallery "restyle" was a paper restyle — `GalleryManager.tsx` was still 1433 lines of dashboard-era `bg-white` / `border-gray-300` / `text-gray-600` / `text-blue-400` / `bg-blue-50` with only a `<Lightbox>` imported on top. Full sweep: 158+ call sites replaced with semantic tokens — `bg-gray-*` → `bg-(--surface-*)`, `border-gray-*` → `border-(--border-*)`, `text-gray-*` → `text-(--text-*)`, `bg-blue-*` / `text-blue-*` / `focus:ring-blue-*` → `--brand-fg` equivalents, `bg-emerald-*` / `text-emerald-*` / `bg-red-*` / `bg-amber-*` / `bg-green-*` to transparent-fill + `dark:` text forks so semantic colors survive dark mode. Amber featured-badge + cover-photo pill kept as solid amber-500 (intentional accent). **Lightbox** `transition={{ duration: 0.15 }}` and `0.16` updated to include the locked easing `ease: [0.22, 1, 0.36, 1]` so motion language stays single-curve. Upload/CRUD/HEIC/folder-drop logic untouched. **Deferred to Phase 3.1:** Lucide-ify the ↑/↓/✕ ascii controls, convert upload form to `<GroupedList>` sections, iOS-style project grid layout. The daily-driver read improvement from this pass (dark-mode parity + consistent tokens) is the P0 win. |
| 2026-04-24 | **HQ Phase 4 scope: voice memo → lead only (Camera + Receipt OCR deferred)** | Original Phase 4 plan (commit `a9672d3`) bundled three features: voice memo → lead, camera-first job photos, receipt OCR → QBO. Julian scoped this session to VOICE ONLY to ship the highest-value capability end-to-end before stacking features. Camera + Receipt become Phase 4.1 + 4.2. Reasons: (1) voice memo is the most-used daily capability — 10x more leads than receipts; (2) it has independent infra (no QBO API surgery); (3) derisks the Anthropic-API integration path for the subsequent OCR work. |
| 2026-04-24 | **HQ Phase 4 plan revision: Claude doesn't accept direct audio — use Whisper + Claude two-step** | Original plan assumed the Anthropic Messages API supported direct audio input ("Anthropic audio transcription"). It does not (verified via claude-code-guide agent against platform.claude.com/docs). Revised pipeline: browser MediaRecorder (iOS → audio/mp4 AAC) → `/api/hq/voice-lead` → **OpenAI Whisper** (`whisper-1`, $0.006/min, ~$0.30/mo at 100 memos) transcribes → Claude Sonnet 4.6 extracts structured fields from the transcript → validated insert into `leads`. Chose Whisper over Deepgram ($0.0043/min) because Whisper has better accent handling + is the de-facto standard; the $0.09/mo delta is immaterial. New env var `OPENAI_API_KEY` (added to `.env.example` with setup comment). `ANTHROPIC_API_KEY` already used silently by `permit-extractor.ts`. |
| 2026-04-24 | **HQ Phase 4 entry point: hold-to-record on the existing + button** | Three entry-point options were surfaced (hold-to-record on `+` · dedicated mic tile in CreatePopover · floating mic button on Today). Julian picked hold-to-record per the original plan — matches the iMessage walkie-talkie mental model, no new UI real estate, no bottom-nav changes. **Press behavior:** `onPointerDown` arms a 500ms long-press timer (matches iOS system long-press). Release <500ms = toggle CreatePopover (unchanged behavior, preserves the 5-option tile grid). ≥500ms = armRecording → `getUserMedia` → overlay appears → MediaRecorder begins. Release during recording = stop + upload + transcribe + navigate to `/hq/leads/[id]`. Overlay X or Escape = cancel + discard. pointerCancel during recording treated as release-to-send (handles iOS system alerts mid-recording). |
| 2026-04-24 | **Voice-memo fallback-on-failure policy: save transcript, never lose audio** | If Claude extraction fails (invalid JSON, schema mismatch, API down), the server endpoint still inserts a lead with the raw transcript as the message field and `source='voice_memo'`, flagged in the response as `warning`. Rationale: Julian's voice memo represents real prospect info — losing it because Claude returned malformed JSON would erode trust in the feature within the first week. The lead appears in `/hq/leads` as "Voice memo (extraction failed)" with full transcript, Julian edits fields inline. Insert failure (DB unavailable) returns 500 + error so the client keeps the audio blob for retry. |
| 2026-04-24 | **No `leads.source` schema migration needed — column is free text** | Original plan called for `014_leads_source_voice_memo.sql` to extend a CHECK constraint. Verified against `001_initial_schema.sql`: `source text default 'website_form'` has no CHECK constraint. FB webhook already inserts `'facebook_lead_ads'` / `'facebook_messenger'` without issue. Inserting `'voice_memo'` works as-is. Migration skipped. Phase 4.1 / 4.2 may still need migrations for `job_photos_log` + `job_receipts`. |
| 2026-04-24 | **HQ Phase 4.1 scope: camera-first job photos (Receipt OCR → QBO deferred to 4.2)** | Second sub-phase of the original Phase 4 plan. Photo capture on /hq/jobs/[id] uses iOS's native rear-camera shortcut via `<input capture="environment">` (no getUserMedia permission dance — lighter than voice memo's MediaRecorder). Client-side image prep (`createImageBitmap({ imageOrientation: 'from-image' })` for EXIF auto-orient, OffscreenCanvas resize to 2048px long edge, JPEG 85%) before upload so a 48 MP HEIC → ~400 KB over LTE. Server inserts into existing `gallery_items` + `gallery_photos` two-table structure; photos stay private (`is_active = false`) so they don't leak into the public `/gallery`. Julian can later toggle `is_active=true` per-project via `/hq/gallery` if a job becomes portfolio-ready. |
| 2026-04-24 | **Photo-to-job linkage: add `gallery_items.job_id` FK (skip dedicated `job_photos_log` table)** | Original plan called for a separate `job_photos_log` table with phase (foundation/framing/panels/complete) + lat/lng/uploaded_by audit fields. Cut that scope: (1) `gallery_items.job_id` nullable FK is the simplest correct model — one gallery_item per job, many gallery_photos under it (matches the existing per-project photo structure used for portfolio shots). (2) Phase-tagging + geolocation are nice-to-haves that haven't been requested; defer to a follow-up. (3) Audit trail (uploaded_at, uploaded_by) is already covered by Supabase's built-in `created_at` + RLS auth context. Migration `012_gallery_items_job_id.sql` — `add column if not exists job_id uuid references public.jobs(id) on delete set null` + filtered index. Idempotent. `ON DELETE SET NULL` so deleting a job keeps the photos (Julian may want them for the portfolio even after job close). |
| 2026-04-24 | **Photos endpoint: one `gallery_item` per job, append `gallery_photos`** | `/api/hq/job-photo` POST finds-or-creates a single `gallery_items` row per `job_id`; each subsequent photo appends a row to `gallery_photos` under that item. Title auto-generated as `"{Customer} — Job {job_number}"` (or `"Job {job_number}"` if no customer). `type` mapped from `jobs.job_type` (carport→Carport, garage→Garage, etc.), `tag` from `jobs.structure_type` (welded→Welded, bolted→Bolted). First photo of a fresh item becomes `is_cover`; subsequent photos keep existing cover untouched (Julian can swap covers in `/hq/gallery`). Rollback on failure: if `gallery_photos` insert fails AFTER the item was just created, delete the item + storage blob so the DB stays clean. Storage path: `jobs/{job_id}/{timestamp}.{ext}` in the existing `gallery` bucket. |
| 2026-04-24 | **Camera UX: `<input capture="environment">` + multi-select, no live MediaRecorder** | Unlike voice memo (hold-to-record MediaRecorder), camera uses the iOS-native camera sheet invoked by `<input type="file" accept="image/*" capture="environment" multiple>`. Rationale: iOS's photo picker is faster, higher-quality, has zoom/flash/portrait-mode built in, and doesn't require a getUserMedia permission grant per session. Multi-select lets Julian shoot a burst of 3-5 photos on a jobsite visit and release — parallel upload with per-file status chips (prepping → uploading → done ✓ / failed ✗ with dismiss). Burst cap of 6 in-flight to keep Vercel function limits + LTE bandwidth manageable. |
| 2026-04-24 | **HQ Phase 4.2 scope: receipt OCR → QBO Purchase + Attachable** | Final piece of the original Phase 4 plan. Tap **Receipt** on `/hq/jobs/[id]` → iOS native camera sheet → Claude Sonnet 4.6 vision extracts vendor/date/subtotal/tax/total/line_items + confidence (0..1) → editable confirmation sheet (rebuild fields, line items editor, low-confidence warning banner) → POST to `/api/hq/receipt/[id]/confirm` → QBO Purchase create + Attachable upload (image attached to the expense). Cost: ~$0.02/receipt × 200/mo = $4/mo. Sibling component to `JobPhotoStrip` on the job detail page. |
| 2026-04-24 | **Receipt-to-QBO category strategy: single configurable account (`qbo_tokens.expense_account_id`)** | Three options were on the table: single fixed account · per-receipt account picker · auto-suggest with override. Locked single fixed account. Reasoning: (1) every Triple J jobsite receipt is functionally "Job Materials" — Lowe's, MetalMax, Sherwin Williams, fuel — re-categorization in QuickBooks is a rare exception. (2) Per-receipt categorization adds friction at point of capture (one extra tap × every receipt). (3) Auto-suggest depends on Claude correctly mapping vendor → COA which we haven't validated against Triple J's actual chart yet. (4) Single-account ships fastest and is the only choice that avoids needing a `/api/qbo/accounts` cache for confirm-time rendering. Account ID stored on `qbo_tokens.expense_account_id`; cached display name on `expense_account_name` (added in migration 013). Configured once via the new `ExpenseAccountPicker` on `/hq/settings/quickbooks`. Re-categorize in QBO if needed. |
| 2026-04-24 | **Receipt failure mode: save-local + retry button (never lose a confirmed receipt)** | If QBO is disconnected, the `expense_account_id` is unset, or the QBO API errors mid-push, the user's edits + extracted data are saved to `job_receipts` with `qbo_pushed_at = null` + `qbo_push_error = "<message>"`. The confirmation sheet returns a friendly "Saved locally — connect QBO under Settings to push" message; the receipt appears in the strip with a red "Push failed" pill. `/hq/settings/quickbooks` shows "N receipts pending push" with a single-tap batch retry that calls `POST /api/hq/receipts/push-all` (sequential push of every `qbo_pushed_at IS NULL` row, surfacing per-row failures so Julian sees what's still wrong). Mirrors the voice-memo extractor's fallback policy: never lose work to a transient infrastructure issue. |
| 2026-04-24 | **Receipt OCR endpoint shape: extract-and-save first, /confirm second** | Two-step server flow lets the user verify before committing to QBO. POST `/api/hq/receipt` uploads image to Supabase Storage (bucket `gallery`, path `jobs/{job_id}/receipts/{ts}.{ext}`), runs Claude vision, persists a `job_receipts` row with `qbo_pushed_at = null`, returns `{id, image_url, extracted}`. Client opens the editable confirmation sheet (auto-populated from `extracted`). POST `/api/hq/receipt/[id]/confirm` accepts the user-edited body, applies edits to the row, then attempts the QBO push. If extraction fails entirely (Claude returns invalid JSON, schema mismatch), endpoint still saves a placeholder row with the image so Julian can edit fields manually — same fallback discipline as voice memos. |
| 2026-04-24 | **Receipt UX confidence threshold: amber "Verify" pill below 0.7, no auto-block** | Claude returns `confidence: number (0..1)` per its rubric (penalises blurry/glare/cropped photos, rewards crisp itemized receipts where math reconciles). Below 0.7 the receipt list pill flips amber "Verify" and the confirmation sheet shows a yellow banner ("Low extraction confidence. Double-check the totals…"). User is NEVER blocked from posting — Julian's judgement always wins. Mirrors the voice-memo decision to never gate workflow on confidence, just surface the signal. |
| 2026-04-24 | **QBO client extension: `listExpenseAccounts`, `createExpense` (Purchase API), `uploadAttachable` (multipart)** | `src/lib/qbo.ts` extended with three helpers reusing the existing `getValidAccessToken()` token-refresh path. `listExpenseAccounts` queries `Account` with `AccountType IN ('Expense', 'Cost of Goods Sold', 'Other Expense')` so the dropdown isn't polluted with Asset/Liability accounts. `createExpense` posts a Purchase entity with `PaymentType: 'Cash'`, `AccountRef` on every line, optional `EntityRef` (find-or-create vendor by display name, mirrors `findOrCreateCustomer` pattern), optional `TxnDate` and `PrivateNote`. `uploadAttachable` posts to QBO's multipart `/v3/company/{realm}/upload` endpoint with `file_metadata_0` (JSON) + `file_content_0` (binary), linked to the Purchase via `AttachableRef`. Attachable failure is non-fatal — Purchase already exists. |
| 2026-04-25 | **Native iOS deferred — invest in PWA perf instead** | Asked: go native SwiftUI for the HQ app, or stay on the PWA and make it faster? Locked: stay on PWA, run a structured perf push. Reasons: (1) PWA already does 95% of native (install-to-home, voice memos, camera, push, offline) — no specific pain point named that requires native APIs. (2) Native = second codebase forever, App Store review cycle (2-7 days vs current 30 seconds via Vercel). (3) Operations workflow is the moat, not the platform — voice → lead, camera → photo, receipt → QBO are workflow wins. (4) Going native locks out the `git push → live in 30s` superpower the solo build velocity depends on. Revisit only if a measured perf win can't close a specific pain point — none currently named. |
| 2026-04-25 | **Perf push baseline: 1.3 MB chunk turned out to be heic2any (already lazy)** | Initial planning hypothesis was that a single 1.3 MB JS chunk shipped to every visitor and was the primary perf villain. Read `.next/static/chunks/` directly during Phase 0 measurement and confirmed: that chunk is heic2any, gated behind `await import('heic2any')` in `src/lib/heic-convert.ts:30` since it was first added. Loads only when a user picks a HEIC file. Real first-load JS is ~1.0 MB on HQ pages, ~880 KB on marketing pages. Phase 1 win was ~150 KB shaved (framer-motion + hCaptcha lazied) — smaller headline than the original "1.3 MB → 500 KB" pitch but real and compounding. Documented in `docs/PERF-BASELINE-2026-04-25.md` so future audits don't repeat the mistake. |
| 2026-04-25 | **Perf push: 4-phase plan, all phases shipped** | Phase 0 (baseline doc), Phase 1 (lazy-load Sheet + Lightbox + hCaptcha via `next/dynamic`), Phase 2 (Suspense streaming on `/hq/leads/[id]` + `/hq/customers/[id]` + `/hq/jobs/[id]`; `<Link prefetch>` on every inbox row; shared `<Skeleton>` primitives extracted), Phase 3 (`/hq/leads` paginated 500 → 50 with split status-only count query; `notifyNewLead()` moved to Next's `after()` so voice-memo response ships before email/push fanout; migration 021 adds 7 perf indexes), Phase 4 (8 hero JPEGs recompressed via `sips --resampleWidth 1600 -s formatOptions 65`, ~30% reduction; unused `logo-lion-detailed.png` (1036 KB) deleted; ~2.6 MB total saved off public asset weight). Phase 5 re-measure waits 24h for Speed Insights field data. Plan + per-phase verification in `~/.claude/plans/you-re-working-in-the-joyful-kay.md`. |
| 2026-04-25 | **CSS-only `SwipeActions` deferred** | Last 128 KB of framer-motion still on `/hq/leads` first-load comes from `SwipeActions`. Rewriting to pure pointer events + CSS transforms is doable but failure mode is "swipe-to-action regresses on iPhone Safari" — production-broken-pager risk on the daily-driver leads inbox. Moved to `project_strategic_backlog.md` memory. Revisit with a focused session that includes iPhone QA. |
| 2026-04-25 | **Twilio SMS quote-send: stub now, env vars later (CallRail pattern)** | `src/lib/twilio.ts` is a fetch-based Twilio client (no SDK dep) that returns `not_configured` until `TWILIO_ACCOUNT_SID` + `TWILIO_AUTH_TOKEN` + `TWILIO_FROM_NUMBER` land in Vercel env. `POST /api/quotes/[id]/send-sms` ships a quote summary + accept-token deep link; auto-bumps quote status to `sent` mirroring the email-send route. UI button on `/hq/quotes/[id]` (added 2026-04-25 A3.1). Mirrors the CallRail call-tracking pattern: build the infrastructure now, plug the account in 10 minutes once provisioned, no code changes required at activation. |
| 2026-04-25 | **CallRail call-tracking infrastructure: per-source phone swap with canonical schema fallback** | Built `src/lib/call-tracking.ts` (pure detection logic — UTM > gclid/fbclid > referrer-host heuristics → 12 source buckets) + `src/components/site/TrackedPhone.tsx` (client components: `useTrackedPhone()` hook via `useSyncExternalStore`, `<TrackedPhoneNumber />`, `<TrackedPhoneLink />`, `<TrackedPhoneButtonLink />`). Migrated all 22 user-facing phone CTAs across Header / Footer / MobileCallBar / PreFooterCta + 16 marketing pages + PartnerInquiryForm. **Schema.org telephone stays canonical** — the LocalBusiness `telephone` in `OrganizationJsonLd` and per-page schema must match Google's GBP record; tracking-number swaps live only in customer-facing UI. Logs `phone_displayed` (every mount with identifiable source) + `phone_clicked` (every tap, with `surface=...` attribution) to Vercel Analytics. `TRACKING_NUMBERS` map is empty until CallRail signs up; full runbook in `docs/CALL-TRACKING.md`. |
| 2026-04-25 | **Quote calculator placeholder pricing strategy** | `src/lib/quote-pricing.ts` ships the full calculator engine (`BASE_RATE_PER_SQFT`, door / window / wall / concrete constants, height surcharge, column-tier auto-suggest, custom-dimension flag, margin-buffer 10%/15%/20%) with every dollar value as a placeholder marked `TODO_PRICING`. Architecture is locked; only numbers need updating once the 2025/2026 pricing sheet lands. The calculator output flows through `/hq/quotes/new` step 2 into the existing draft-quote pipeline; `internal_notes` field on `quotes` carries the JSON blob `{kind:'calculator', version:1, inputs, result}` for future migration normalization. |
| 2026-04-25 | **Real PDF generation locked: `@react-pdf/renderer` server-side** | Was: customer accept-page Cmd+P. Now: `GET /api/quotes/[id]/pdf` streams a real PDF rendered via `@react-pdf/renderer` (4.x, on the function bundle, zero client-side bytes). `src/lib/quote-pdf.tsx` defines the document — Letter-sized, brand chrome, line-item table, totals block with brand-blue grand total, footer with legal name + tagline + generated timestamp. The `PDF` button on `/hq/quotes/[id]` was a stubbed "Soon" pill; now opens the route in a new tab. Customer-facing `/quotes/[token]` accept page unchanged — Cmd+P → Save still works there for ad-hoc; this endpoint is for HQ to attach a real PDF to email/SMS. |
| 2026-04-25 | **Standalone `/hq/calculator` route — estimator without DB write** | Reuses the wizard's `calculate()` engine + `CalculatorStep` component but doesn't write anything to `quotes` or `quote_line_items`. Useful during phone calls: plug dimensions, read price aloud, decide whether to formalize. For real quote, link points back to `/hq/quotes/new`. Convert-calculator-state-to-quote button is a follow-up (would require sessionStorage handoff). |
| 2026-04-25 | **SEO criticals from 2026-04-24 audit closed** | (1) `/military` title double-brand: was shipping "...Same-Week Installs \| Triple J Metal \| Triple J Metal" because the page-level title already included the suffix and the root layout's `%s \| Triple J Metal` template appended another. Fixed via `title: { absolute: '...' }`. Same fix on Waco / Georgetown / Round Rock `metaTitle` strings in `src/lib/locations.ts` (stripped trailing brand from each, let template add it once). (2) `src/app/not-found.tsx` shipped — slim brand bar + apologetic H1 + 3 explicit recovery links to `/services`, `/locations`, `/contact`. Replaces Next's default unstyled 404. (3) `BreadcrumbList` JSON-LD now on `/blog/[slug]` (was only on `/services/[slug]` + `/locations/[slug]`). (4) Per-page Twitter cards on `/military`, `/services/[slug]`, `/locations/[slug]`, `/blog/[slug]`. (5) Per-page OG image for `/military` via Next file-based ImageResponse API at `src/app/(marketing)/military/opengraph-image.tsx`. (6) `heroImageAlt` filled on all 22 location entries (was 6/22). (7) Five over-60-char titles trimmed: `/services`, `/partners`, `/services/hybrid-projects`, `/service-areas`, `/services/pbr-vs-pbu-panels`. |
| 2026-04-25 | **Migration 021 — HQ perf indexes** | `supabase/migrations/021_hq_perf_indexes.sql`. Additive, idempotent. Seven indexes: `leads (created_at desc)`, `leads (status)`, `leads (status, created_at desc)`, `jobs (status, scheduled_date)`, `jobs (completed_date desc) where completed_date is not null`, `quotes (customer_id, created_at desc)`, `job_receipts (job_id, created_at desc)`. Apply manually in Supabase SQL editor before traffic hits the new `/hq/leads` paginated flow — without it, the `select status` count query falls back to seq scan and is slow but functional. EXPLAIN ANALYZE verification snippets in the migration body. |
| 2026-04-25 | **Data-model audit — 7 proposed migrations 014-020 (NOT YET APPLIED)** | `docs/DATA-MODEL-AUDIT-2026-04-24.md` proposes seven additive migrations to unblock ad-ROI + flywheel reporting: 014 (leads UTM/gclid/fbclid + intent_stage + budget bracket + referring_customer_id + first_response_at), 015 (leads outcomes — won_at/lost_at/lost_reason + status-change trigger), 016 (jobs.contract_signed_date + cached gross_profit/gross_margin), 017 (`job_costs` unified ledger), 018 (`time_entries` labor table), 019 (customers compounding flags — review_asked, review_left, feature_permission, repeat_contact_permission), 020 (`job_receipts.cost_category` enum + auto-roll into job_costs). All written as code blocks in the audit doc; not committed as SQL files. **Need user sign-off before shipping.** |
| 2026-04-26 | **Killed `/service-areas` — folded into `/locations`** | Two near-duplicate hub pages were splitting Google's ranking signal for "central texas service area" queries. `/service-areas` was the styled marketing version; `/locations` was an unstyled raw-HTML SEO list with a dead `/get-a-quote` link. Resolution: deleted `src/app/(marketing)/service-areas/page.tsx`, added permanent 301 in `next.config.ts`, restyled `/locations` to match the marketing design system + absorbed the stats strip and QuoteForm. Footer "View all →" and `sitemap.ts` STATIC_PATHS updated. `/locations` is the canonical hub (semantic root for `/locations/[slug]` children). |
| 2026-04-26 | **Orphan adoption — `/services/hybrid-projects` + blog cross-links** | True orphan `/services/hybrid-projects` (zero inbound links outside sitemap) added to the Resources card on `/services` as "Custom Hybrid Projects". Added optional `relatedPosts?: string[]` field to ServiceData + LocationData types. Populated for: carports, turnkey-carports-with-concrete, metal-garages, barns, hoa-compliant-structures (services); harker-heights, killeen, temple, belton, waco, bell-county (locations). New `<RelatedReading>` component renders an inline mid-content callout (3-card grid, brand-50 bg) before the closer CTA on each adopted page. Why mid-content: user wanted earlier-than-bottom placement to catch considered-buyer scrollers. |
| 2026-04-26 | **REVERSED again: Lone-Star color names DELETED entirely — MetalMax/Sheffield/Turnium technical names are now the single canonical name** | Both prior color-name decisions (2026-04-23 lock to Lone-Star, 2026-04-23 evening reversal back to technical) are superseded. Owner direction: kill the dual-name `legacyName` field entirely. Reasoning: planning to either partner with MetalMax or at minimum keep using their color names as-is (no branding gymnastics). `PanelColor` type simplified — `legacyName` field removed. `standard()` and `premium()` helpers now take just (name, slug, ...flags). All 39 colors use the supplier's technical name as `name` (Burnished Slate, Polar White, Brilliant White, Colonial Red, Hunter Green, etc.). Galvalume + Acrylic-Coated Galvalume unchanged (already technical). DB had only 2 rows with stored color values (slug-form: "galvalume", "hunter-green") so no migration required. `gallery-colors.ts` resolve() simplified — dropped the legacyMatch check since no legacy column to fall back on. **NOTE on broader supplier-agnostic positioning:** the 2026-04-23 lock said "no MetalMax/MetalMart/WeatherXL/Turnium/Sheffield brand names in customer copy on /about, /services, location pages" — that lock is NOT being lifted by this entry. This entry only addresses panel color *display names*. Whether to mention MetalMax explicitly in marketing copy is a separate decision still pending. |
